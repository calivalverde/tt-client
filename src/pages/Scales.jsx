import React from 'react';
import PlaceholderPage from '../components/PlaceholderPage';

const Scales = () => {
  return <PlaceholderPage title="Scales Explorer" description="An interactive tool to visualize and listen to any scale in any key." />;
};

export default Scales;
